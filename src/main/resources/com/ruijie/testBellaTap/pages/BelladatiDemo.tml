<html t:type="layout" title="Belladati API demo"
      xmlns:t="http://tapestry.apache.org/schema/tapestry_5_3.xsd"
      xmlns:p="tapestry:parameter">

    <p>A demo for Belladati API</p>
    <p>${a}</p>
    <p>${s1}</p>
    
    <div><t:actionlink t:id="login">Log in</t:actionlink></div>
    
    <div><t:actionlink t:id="showdataset">Show Dataset</t:actionlink></div>
    
    <div><t:actionlink t:id="authorize">aut</t:actionlink></div>
    
    <t:if test="loggedIn">
    <div>
    	<tr>Dashboard: ${DashboardInfo.size()}</tr>
    	<t:loop source="DashboardInfo" value="dbi">
    		<tr>${dbi.id}</tr>
		</t:loop>
    	
    	<p>Report: ${reportinfo.size()}
    	<t:loop source="ReportInfo" value="rpi">
    		<br>${rpi.id}</br>
		</t:loop>
    	</p>
    	
    	<p>Dataset: ${datasetinfo.size()}
    	<t:loop source="DataSetInfo" value="dsi">
    		<br>${dsi.id}</br>
		</t:loop>
		</p>
	</div>
	</t:if>

</html>
